/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls'
		sh 'cat Jenkinsfile'
		sh 'll'
		sh 'echo "hello world"'
		sh 'pwd'
		sh 'date'
		sh 'tree -L 1'
            }
        }
    }
}
